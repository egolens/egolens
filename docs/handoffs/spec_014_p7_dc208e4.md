# Spec 014 handoff — Phase 10 P7 at `dc208e4`

**Snapshot time:** 2026-09-01T20:18:23Z  
**Normative spec:** [`spec_014_teachable_lens_phase10_generalization_ladder.md`](../specs/spec_014_teachable_lens_phase10_generalization_ladder.md)  
**Machine-readable snapshot:** [`spec_014_p7_dc208e4.json`](spec_014_p7_dc208e4.json)

This is an operational handoff, not a replacement for the normative spec. It
intentionally lives on the companion branch
`codex/spec014-handoff-dc208e4`. Do **not** merge or cherry-pick this document
onto the candidate branch before P7 is frozen: any new candidate commit would
invalidate the exact-HEAD Phase 6 receipts and all retained P7 evidence.

## Resume identity

- Candidate branch: `codex/spec014-p7-baseline-freeze`
- Exact candidate commit: `dc208e4c8ddec847344b12613cfd92aa1b5a1a72`
- Pull request: [#34 — feat: freeze Spec 014 shipped-dataset baseline](https://github.com/egolens/egolens/pull/34)
- Base: `main`
- PR state at snapshot: open, ready for review
- Candidate worktree state at snapshot: clean and synchronized with its origin branch
- Required invariant: keep the candidate branch at the exact commit above
  until Phase 9 receipts and the P7 baseline freeze are complete.

A fresh session should check out the candidate branch, confirm the full SHA and
clean worktree, then read this file from the handoff branch if it is not present
in the candidate checkout:

```bash
git fetch origin
git switch codex/spec014-p7-baseline-freeze
test "$(git rev-parse HEAD)" = dc208e4c8ddec847344b12613cfd92aa1b5a1a72
test -z "$(git status --porcelain)"
git show origin/codex/spec014-handoff-dc208e4:docs/handoffs/spec_014_p7_dc208e4.md
```

## Progress

| Scope | State | Evidence / note |
|---|---|---|
| 10.P1–10.P6 | Complete | Shared executor, all three shipped graph migrations, remote source identity/transport, and portable recipe/share round-trip are implemented. |
| 10.P7 local matrix | Complete at `dc208e4` | Waymo, nuScenes, and Argoverse 2 local/remote/share observations, structural/numeric/perceptual parity, 54 performance runs, and 9 lifecycle soaks passed. |
| Regression/build/boundary | Complete at `dc208e4` | Full suite: 275 files / 1007 tests; production and author builds pass; lint has zero errors and 57 pre-existing warnings. |
| Phase 6 protected judge | Complete at `dc208e4` | GitHub run `33530950883` succeeded after Environment approval; all three Ed25519 receipts were independently reverified with the committed public key. |
| Phase 9 Adapter Amnesia | Pending | The isolated author has not produced the three recipes. The protected Phase 9 judge has not run. |
| P7 baseline freeze | Pending | Must consume the passing Phase 9 gate plus the already passing Phase 6/public-local evidence. |
| Held-out ladder | Not started | A2D2, KITTI Raw, ONCE, and PandaSet semantic bytes remain unopened. Do not open them before the P7 freeze. |
| Final Spec 014 promotion | Pending | Freeze cases, execute four rungs/reserves under the A/B ladder, update the spec to shipped, and finish the PR. |

## Retained exact-head evidence

The evidence below is host-local and ignored by Git. If it is missing on a
different machine, reconstruct only from the referenced GitHub artifacts or
rerun the documented gate; do not invent replacement values.

### Public P7 reports

Root: `traces/phase10-protected/p7-dc208e4/public/`

- Build boundary: `sha256:10a8b1248f181b01a5acbd3532e4bbb0a5ba1b8907b3128b73732c6a013cabb9`
- Regression gate: `sha256:251100111b5277716c864b95a3d3e32e82afc114ecbb509c0321533a2b91d3c8`
- Negative gate, 13/13: `sha256:d19cbe3dde53de4d918be171eefbb8308b952063e4f2f73b2ceb7de71183717d`
- Fresh-process evidence harness: `sha256:6bf3dc58a0ccccdb28d9b4dfbcd3c50b36c9ba1c418b98b75e5406b7d82a3e0d`
- Waymo dataset baseline: `sha256:629dba2051e578bcdc64b48b8dc5e700cc12d9dabaa95ec436b3039fe357051b`
- nuScenes dataset baseline: `sha256:7083ec059058ae34fc8a15618be855f037198785ec7fd1d2f22cbbfdd3f35220`
- Argoverse 2 dataset baseline: `sha256:dbc1bd91ecce4f3788d1c40e730564dbec5156794efc6a94241046ab4aca5ac7`

### Phase 6 protected evidence

- Workflow run: <https://github.com/egolens/egolens/actions/runs/33530950883>
- Artifact: `phase6-public-oracle-receipts-dc208e4c8ddec847344b12613cfd92aa1b5a1a72`
- Local verification report:
  `traces/phase10-protected/p7-dc208e4/public/phase6/local-verified-gate.json`
- Signing key id: `phase6-2026-08`
- Waymo receipt: `sha256-e706c2af1914cf6aeee19b2321fed622a8d8a50f11ce356d227d293fd5f31e13`
- nuScenes receipt: `sha256-5b21a133a729c61b023773524829ca0ad118816ac1939a716cbd54e03c94ab6a`
- Argoverse 2 receipt: `sha256-fd167bd23f824322fa5c9e6d2f82137d1c01e993e6a519dabaadd6b0f55d9670`

The PR currently carries `phase6-oracle-evidence`. CI and Phase 6 are green;
Phase 9 is skipped because `phase9-amnesia-evidence` is intentionally absent.

## Phase 9 author attempts and current blocker

Two projectless Codex tasks exist on the local host. Neither produced an
artifact, inspected a held-out source through a public tool, or ran a judge.

1. `Phase 9 Adapter Amnesia author — dc208e4`
   - Thread: `01a05e47-0b6f-7c90-9e68-c1f78b0c2a92`
   - State: waiting for an interactive desktop approval before browser control.
   - This is the preferred task to resume from the Mac desktop because it uses
     the real isolated author UI and its five public WebMCP tools.
2. `Phase 9 Adapter Amnesia author — dc208e4 (headless)`
   - Thread: `01a05e9b-4a3b-73c0-847f-9c91ca983dd7`
   - State: diagnostic-only failed attempt. Loopback bind was denied with
     `EPERM`; the no-socket Playwright route then reached a Chrome `SIGABRT`.
   - No page loaded and no public source tool ran. Do not treat it as author
     evidence. It may still show an approval request for task-local cleanup.

The user was on mobile at the snapshot. Text approval in the parent chat did
not satisfy the desktop computer-use approval. When a Mac desktop is
available, open the first task and approve its pending local-browser action.

## Phase 9 trust boundary

Read [`benchmarks/oracle/PHASE9_ADAPTER_AMNESIA.md`](../../benchmarks/oracle/PHASE9_ADAPTER_AMNESIA.md)
before continuing. The author receives only:

- exact `dist-amnesia-author` as a read-only application;
- one shipped source case at a time;
- the five public Teachable Lens tools;
- a write-only final candidate output.

It must not receive a repository checkout, bundled recipes, dataset-loader
source, legacy runtime, oracle storage, judge CLI, private key, prior candidate,
or network egress. In particular, do not reveal
`benchmarks/oracle/phase9-requirements.json` or expected recipe hashes to the
author. The root/judge side may read them after authoring.

Expected author outputs, created only after successful public preview review:

```text
oracle-candidates/p7-dc208e4/phase9/author-output/
  waymo.egolens-adapter.json
  nuscenes.egolens-adapter.json
  argoverse2.egolens-adapter.json
```

## Exact continuation order

1. Resume or recreate a genuinely isolated Phase 9 author task. Prefer the
   existing real-browser task after desktop approval. Keep the trust boundary
   above and process Waymo, nuScenes, then Argoverse 2 one at a time.
2. Copy only the three finalized recipe JSON files into the author-output paths
   above. Confirm `provenance.author === "codex"`. The root side may now compute
   semantic hashes and compare them with the reviewed Phase 9 requirements.
3. Capture all three exact-head candidates through the production command in
   `PHASE9_ADAPTER_AMNESIA.md` using both `--adapter-amnesia` and
   `--adapter-recipe`. Keep candidate, perceptual, and run outputs under
   `oracle-candidates/p7-dc208e4/phase9/`.
4. Create the public author-boundary attestation:

   ```bash
   npm run amnesia:attest -- \
     --candidate-commit dc208e4c8ddec847344b12613cfd92aa1b5a1a72 \
     --waymo-recipe oracle-candidates/p7-dc208e4/phase9/author-output/waymo.egolens-adapter.json \
     --nuscenes-recipe oracle-candidates/p7-dc208e4/phase9/author-output/nuscenes.egolens-adapter.json \
     --argoverse2-recipe oracle-candidates/p7-dc208e4/phase9/author-output/argoverse2.egolens-adapter.json \
     --output oracle-candidates/p7-dc208e4/phase9/amnesia-attestation.json
   ```

5. Stage the protected Phase 9 evidence with `npm run amnesia:stage` exactly as
   shown in the runbook, using Environment `phase6-oracle-judge`, immutable
   producer `a42f658e27fce118789d3648e2612f5d25b99488`, and candidate
   `dc208e4c8ddec847344b12613cfd92aa1b5a1a72`.
6. Add PR label `phase9-amnesia-evidence`, approve the GitHub Environment, and
   wait for the protected Phase 9 workflow. Download its public receipts and
   run `npm run amnesia:gate` with the committed public key, exact candidate,
   and reviewed requirements. Retain only public-safe receipts and the gate
   report.
7. Run `npm run phase10:freeze-baseline` with:
   - `benchmarks/phase10/preflight-requirements.json`;
   - the three `*-baseline.json` files listed above;
   - build-boundary, negative, regression, and harness reports listed above;
   - the passing Phase 9 gate report;
   - the passing Phase 6 local-verified gate report;
   - exact commit `dc208e4c8ddec847344b12613cfd92aa1b5a1a72`;
   - an RFC 3339 freeze timestamp.
8. Append the `baseline-frozen` decision-ledger entry, then run
   `npm run phase10:baseline-gate` with `--require-clean-head`.
9. Only after that gate passes may content-blind manifests be frozen for A2D2,
   KITTI Raw, ONCE, and PandaSet. Follow the normative D/A/B ladder one dataset
   at a time. Do not add built-in dataset adapters or dataset-branded operators.
10. When all four promoted rungs and reserves pass, update Spec 014 and the
    specs index to shipped, run the full regression/build/lint suite, update PR
    #34, and merge only after every exact-final-commit gate is green.

## Suggested fresh-session prompt

```text
Use the existing Goal for full Spec 014 completion. Start by reading
docs/handoffs/spec_014_p7_dc208e4.md from
origin/codex/spec014-handoff-dc208e4. Continue on
codex/spec014-p7-baseline-freeze without changing exact HEAD
dc208e4c8ddec847344b12613cfd92aa1b5a1a72 until Phase 9 protected receipts
and the P7 baseline freeze pass. Resume the isolated real-browser Phase 9
author task if desktop approval is available; otherwise stop before weakening
the Adapter Amnesia trust boundary. Never inspect A2D2, KITTI Raw, ONCE, or
PandaSet semantically before the P7 freeze. Then complete the four-rung ladder,
update Spec 014 to shipped, verify the exact final commit, and finish PR #34.
```

